{
  "name": "education-craft",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler deploy",
    "generate:audio": "node scripts/generate_audio_assets.mjs",
    "generate:version": "node scripts/generate_version_snapshot.mjs",
    "capture:debug": "node scripts/capture_debug_screenshots.mjs",
    "playwright:install": "playwright install chromium"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^7.0.1",
    "@sveltejs/kit": "^2.52.0",
    "playwright": "^1.55.0",
    "svelte": "^5.51.1",
    "vite": "^7.3.1",
    "wrangler": "^4.65.0"
  },
  "dependencies": {
    "three": "^0.179.1"
  }
}
