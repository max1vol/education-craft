export const keyFor = (x: number, y: number, z: number): string => `${x},${y},${z}`;
export const columnKeyFor = (x: number, z: number): string => `${x},${z}`;
export const chunkKeyFor = (cx: number, cz: number): string => `${cx},${cz}`;
